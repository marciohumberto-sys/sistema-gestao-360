const { createClient } = require('@supabase/supabase-js'); const dotenv = require('dotenv'); dotenv.config({path: '.env.local'}); const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_ANON_KEY); async function test() { const { data: ut } = await supabase.from('user_tenants').select('tenant_id').limit(1); if(ut && ut.length > 0) { const { data } = await supabase.rpc('get_farmacia_users_with_auth', { p_tenant_id: ut[0].tenant_id }); console.log(JSON.stringify(data.map(r => ({id: r.user_id, email: r.email, unit: r.unit_code})), null, 2)); } } test();
